apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maskshop.fullname" . }}-test-connection"
  labels:
    {{- include "maskshop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maskshop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
