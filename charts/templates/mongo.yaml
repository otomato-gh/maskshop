apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Values.appsVars.appNameMongo }}
  name: {{ .Values.appsVars.appNameMongo }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.appsVars.appNameMongo }}
  template:
    metadata:
      annotations:
      labels:
        app: {{ .Values.appsVars.appNameMongo }}
    spec:
      containers:
        - image: "{{ .Values.appsImages.appRepoMongo }}:{{ .Values.appsImages.appRepoTagMongo }}"
          name: {{ .Values.appsVars.appNameMongo }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.appsVars.appPortMongo }}
              protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.appsVars.appNameMongo }}
  name: {{ .Values.appsVars.appNameMongo }}
spec:
  ports:
  - port: {{ .Values.appsVars.appPortMongo }}
    protocol: TCP
    targetPort: {{ .Values.appsVars.appPortMongo }}
  selector:
    app: {{ .Values.appsVars.appNameMongo }}
  type: ClusterIP